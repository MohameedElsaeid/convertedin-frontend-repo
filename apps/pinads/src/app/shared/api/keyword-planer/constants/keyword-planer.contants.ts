import { ApiConstants } from '@pinads/shared/constants/api/api.constant';

export class KeywordPlanerEndpoints {
  /**
   * get brands endpoint URL.
   */
  static readonly GET_KEYWORD_PLANER =
    ApiConstants.INITIAL + ApiConstants.VERSION(1) + '/keyword';


}
