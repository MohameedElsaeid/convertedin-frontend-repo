export enum TransactionType {
  RECHARGE = 1,
  AD = 2,
  PROMO_CODE = 3,
  REVIEW = 4,
  DASHBOARD = 5,
  REFUND = 6,
  REFERRAL = 7,
  BLOCKED_AMOUNT = 8,
  ALL = '',
}

export enum TransactionState {
  IN = 'in',
  OUT = 'out',
}
